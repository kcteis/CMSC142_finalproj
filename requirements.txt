# Barangay Relief Pack Optimizer — Python dependencies
# Python 3.9+ required (uses PEP 585 generic type hints like list[Family]).
# tkinter is part of the Python standard library and is NOT pip-installable.
# On Linux you may need to install it via your package manager, e.g.:
#   sudo apt install python3-tk
#
# The core app (knapsack + greedy + GUI) uses only the Python standard library.
# No pip packages are required.
